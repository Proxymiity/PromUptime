# Python-based Prometheus Monitoring/Uptime Exporter

Clone-and-Play HTTP and ICMP basic monitoring.  
This script query nodes in the ``config.json`` file. You can use ICMP (faster) or HTTP[s] (slower).